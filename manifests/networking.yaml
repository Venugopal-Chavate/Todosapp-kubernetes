---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todos-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-methods: "PUT, GET, POST, OPTIONS, DELETE"
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /api/todos/$1  # Changed to preserve /api/todos path
spec:
  rules:
  - http:
      paths:
      - path: /api/todos(/|$)(.*)  # This will match /api/todos and /api/todos/
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 3000
      - path: /(.*)
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
# ---
# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: mongodb-network-policy
# spec:
#   podSelector:
#     matchLabels:
#       app: todos
#       tier: database
#   policyTypes:
#   - Ingress
#   ingress:
#   - from:
#     - podSelector:
#         matchLabels:
#           app: todos
#           tier: backend
#     ports:
#     - protocol: TCP
#       port: 27017